var numero = 1; // Inicializamos la variable numero en 1
var suma = 0; // Inicializamos la variable suma en 0

while (numero <= 5) {
  suma += numero; // Agregamos el valor actual de numero a la suma
  numero++; // Incrementamos el valor de numero en 1
}

alert("La suma de los números del 1 al 5 es: " + suma);
